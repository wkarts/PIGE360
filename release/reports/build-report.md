# Relatório final de builds

Backend/OpenAPI/SDK/migrations: **aprovados**.

Frontend: fontes de 13 apps aprovadas em TypeScript estrito; bundle Vite local **não executado** por ausência das dependências npm no cache local.

Docker/Rust/Android/iOS: não executados neste host por ausência das toolchains.
