# Diretório de secrets em runtime

Este diretório não contém segredo. Execute `scripts/local/init-secrets.sh runtime-secrets` na instalação. Arquivos `.txt` gerados localmente são ignorados e nunca devem entrar em pacote ou versionamento.
