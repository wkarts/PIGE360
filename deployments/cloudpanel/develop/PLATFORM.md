# PIGE360 develop no Cloudpanel

Importe `compose.yaml`, copie as variáveis de `.env.example` e mantenha o projeto com nome estável. Todo o ciclo de inicialização é executado pelos services do stack.
