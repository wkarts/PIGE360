# Branding

Substitua assets em `src/assets/branding` e `src-tauri/icons` mantendo dimensões e transparência.

Depois revise `brand.json`, `package.json`, `src-tauri/tauri.conf.json`, `index.html`, `README.md`, `VERSION` e `release.config.mjs`.
